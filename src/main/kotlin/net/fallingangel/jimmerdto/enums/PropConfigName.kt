package net.fallingangel.jimmerdto.enums

enum class PropConfigName(val text: String) {
    Where("where"),
    OrderBy("orderBy"),
    Filter("filter"),
    Recursion("recursion"),
    FetchType("fetchType"),
    Limit("limit"),
    Offset("offset"),
    Batch("batch"),
    Depth("depth"),
}